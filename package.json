{
  "name": "ai-chatapp-mern-gemini",
  "version": "1.0.0",
  "description": "Full-Stack ChatGPT-like AI Chat Application built with MERN & Gemini API",
  "scripts": {
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "build": "npm --prefix client run build"
  },
  "keywords": ["mern", "chatgpt", "gemini-api", "react", "nodejs", "express", "mongodb"]
}
